# Application Port
PORT=5001 # Or another preferred port

# MongoDB Connection String (replace with your actual URI later)
MONGODB_URI=mongodb://localhost:27017/investor_portal_dev

# JWT Secret Key (replace with a strong, random secret later)
JWT_SECRET=YOUR_VERY_SECRET_KEY_REPLACE_ME